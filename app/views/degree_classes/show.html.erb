<h1><%= @degree.degreeyr %> - <%= @degree.title %></h1>

<% if @requirements.empty? %>
  There are no requirements for this degree.
<% else %>
  Options:
  <% @requirements.each do |r| %>
    <h3><%= r.lecture_course.code %> - <%= link_to r.lecture_course.title, r.lecture_course %> (<%= r.required %>)</h3>
  <% end %>
<% end %>
