<h1>Staff Members</h1>

<ul>
  <% @staff.each do |s| %>
    <li>
      <h4>
        <%= link_to "#{s.login} - #{s.full_name}", year_staff_member_path(@year, s) %>
      </h4>
    </li>
  <% end %>
</ul>
